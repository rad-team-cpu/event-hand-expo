import { InboxScreenProps } from '@/routes/types';
import React from 'react';
import { Text } from 'react-native-paper';

const Inbox = (props: InboxScreenProps) => <Text>Inbox</Text>;

export default Inbox;
